<?xml version="1.0" encoding="utf-8"?>
<!--
  The dropdown row for one autofill suggestion.

  android.R.layout.simple_list_item_2 was the obvious thing to reach for and is
  silently fatal: its root is TwoLineListItem, which is not annotated @RemoteView,
  so RemoteViews refuses to inflate it and the platform logs "Class not allowed to
  be inflated" on its own thread. The service still reports success, so nothing
  looks wrong from this side — the suggestion simply never appears. Only
  LinearLayout, TextView and the other @RemoteView classes are safe here.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <TextView
        android:id="@+id/autofill_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="16sp"
        android:textColor="?android:attr/textColorPrimary" />

    <TextView
        android:id="@+id/autofill_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="14sp"
        android:textColor="?android:attr/textColorSecondary" />
</LinearLayout>
